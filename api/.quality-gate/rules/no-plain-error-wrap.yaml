id: no-plain-error-wrap
language: Go
severity: error
message: >
  Wrap errors with %w instead of %v or %s so the original error is preserved
  for errors.Is / errors.As unwrapping.
rule:
  pattern: fmt.Errorf($FORMAT, $ERR)
constraints:
  FORMAT:
    regex: "%[vs]"
    not:
      regex: "%w"
