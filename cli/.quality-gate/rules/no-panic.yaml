id: no-panic
language: Go
severity: error
message: >
  Avoid panic() in production code. Return an error instead so callers can
  handle the failure gracefully.
rule:
  pattern: panic($$$ARGS)
ignores:
  - "**/*_test.go"
