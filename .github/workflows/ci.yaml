name: Quality Gate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality-gate:
    name: Quality Gate
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup devenv
        uses: prefeitura-rio/actions/setup-devenv@master

      # TODO: replace the placeholder below with the quality gate action once available.
      #
      # - name: Run quality gate
      #   uses: prefeitura-rio/actions/quality-gate@master
      #   with:
      #     language: go

      - name: Quality gate (placeholder)
        run: echo "Quality gate not yet wired up. See prefeitura-rio/actions."
